# Build output
/node_modules/
/dist/
/target/
/src-tauri/gen/schemas/

# The concept paper lives outside the repository on purpose: it is the working
# document, and it names paths and people that a public repository need not.
/amberbeam.html

# Keys for the SFTP test server. Generated by dev/test-sftp-server.sh, never
# committed: a private key in a repository is a bad habit even when it guards
# nothing.
/dev/sftp-test/keys/

# macOS
.DS_Store
