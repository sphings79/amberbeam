{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What every window may do. The site manager and each editor window are windows of their own with the same rights: they talk to the same core through the same commands, and leaving them out would have left every one of their calls refused.",
  "windows": [
    "main",
    "sites",
    "editor-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "dialog:allow-open",
    "updater:default"
  ]
}
